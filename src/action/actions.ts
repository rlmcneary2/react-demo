

import * as image from "./image";


const types = Object.freeze(Object.assign(
    {},
    image.types
));

const actions = Object.freeze(Object.assign(
    {},
    image.actions
));

export { actions, types };
